+++
locale = "la"
+++

# Negative: a missing export fails after the namespace resolves.
# Expected: SEM004.namespace_missing_export.

importa ex "tela:tela" privata * ut tela

functio probe() → textus {
    fixum tela.View visus ← tela.text_view("salve")
    redde tela.html_visus_ABSENT(visus) vel ""
}

incipit {
    nota probe()
}
